import { CalPerServPipe } from './cal-per-serv.pipe';

describe('CalPerServPipe', () => {
  it('create an instance', () => {
    const pipe = new CalPerServPipe();
    expect(pipe).toBeTruthy();
  });
});
